"use strict";
function make_shirt(size, message) {
    return (`You got ${size} size shirt with "${message}" printed on it`);
}
let result = make_shirt("small", "welcome to GIAIC");
console.log(result);
